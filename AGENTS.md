# 工作约定

适用于 `/home/shaoyuantian/program/HiF4_Sp` 及其 CLI、编辑器插件和代理。默认中文沟通，命令、路径、代码标识符保留原样；完整规范只维护在本文件 `AGENTS.md`。

通用流程参考本机 `/home/shaoyuantian/program/VAELLM/AGENTS.md`，按条款独立适配。跨项目任务先核实目标目录，不因终端/IDE 目录不同而改错项目；来源只作参考。共用规则以本工作区为准，客户端连接配置/平台限制留在客户端，不整文件互相覆盖。

## 一、通用行为规则

### 1. 从需求出发，保持任务范围

- 从需求、原因和验收目标出发，先查源码、依赖与记录，不套模板、不盲从既有实现或用户的方法。目标清晰时直接执行，只问影响结果的实质歧义。
- 疑似笔误/误记与事实、约定或上下文冲突且影响结构、行为或结果时，先说明并确认意图，不机械另建流程或默默替换方案；只暂停依赖部分。
- 已明确要求不重复确认，已解决问题不反复讨论。只问关键问题；收集长期偏好时一次问一个普通文本问题并等明确答案，推荐项、沉默或超时不算确认。
- 指出前提矛盾、因果跳跃、口径不一或结论越界，说明依据与修正，区分事实、解释和假设。
- 有助决策时提一两项省去重复劳动/等待的建议，说明收益、代价和证据/估计；权限内可采用等价省事方式，建议不授权改方案，不重提已否决方案。
- 不擅自增功能或扩大范围；无关且不影响结果的问题仅简短提醒，不顺手修改。

### 2. 修改范围与实现方式

- 做满足需求的最小完整改动，保留现有结构；结构性重构须明确要求，不能牺牲算法正确性、语义或关键调用链。
- 可修复任务必需的关联 bug，交付时说明根因和改法；仍遵守算法、接口、环境和 Git 确认边界。
- 仅移除本次替代、确认无用且不影响用法的废弃代码，不扩大到其他历史代码或必要长期测试。
- 只为当前合理用法添加少量不破坏现有接口的参数，不为假想需求预留开关、抽象或扩展机制。
- 不新增旧实现兼容分支，不以叠补丁、降级或启发式后处理代替根因修复，不用 mock/stub 或无关依赖掩盖问题。

### 3. 必须先确认的操作

任务内只读检查、代码/配置/日志/结果读写及必要测试、GPU 验证可自主执行；正式训练、全量评测和长实验按任务授权范围执行。以下操作须说明必要性、内容和影响并取得明确同意，已有具体授权时不重复询问。

- 改变已确认算法定义、目标函数、采样规则或评测口径；修回已确认设计不等于重新定义算法。
- 删除/重命名 CLI 参数、改变含义或使已有命令失效。
- 安装、升级、降级、卸载依赖或修改依赖声明；改变驱动/CUDA/PyTorch、系统配置或改用未经确认的环境。激活 `hif4` 可自主完成。
- 创建/切换分支或 worktree、`git add`、提交、推送；计划/skill 的 Commit 步骤或“写提交说明”不构成执行 Git 的授权，默认只改当前工作区。
- 停止既有进程；仅本轮自启的一次性临时验证进程可自行停止。停止监控不等于停止实验。
- 覆盖其他任务产物、干扰其他任务、占用明显超出授权范围的资源或其他不可逆/高影响操作。删除遵守第八节。

“任务必需”、建议或规范更新不扩大权限；只暂停依赖新授权或缺失信息的步骤，继续独立工作。

### 4. 算法语义与工程差异

- 不为省时、加速或省显存改变已确认算法、数据语义、实验协议或省略关键验证；区分算法正确性、数据/流程完整性和底层实现差异。
- 修改前理解受影响调用链：shell 参数、CLI、入口、数据/模型、校准/训练、量化/重构、导出保存与评测；修改后核对输入、输出、保存格式和调用方，不扩成无关审计。
- 算法/协议不变时，可依据明确误差分析和针对性验证自主调整计算精度并说明影响；精度是固定条件或实验变量时按原协议处理。
- 并行、归约/累加顺序、舍入或等价计算核的正常浮点差异，默认不要求逐位一致，不因此阻断实验或添加补偿；不把实质异常归咎于浮点误差。
- 冻结状态、packed bits、checkpoint 字段/拓扑、exact-resume 和保存重载一致性等明确契约仍须验证；一般浮点容差不能取消严格契约，局部严格要求也不能扩成全链路逐位一致。
- 记录可能影响结论的差异与范围。已有证据表明在允许范围内时不补验；超出所需精度、改变比较结论或证据不足时，只针对具体影响补验。

### 5. 错误处理：尽早、明确地失败

- 入口只做必要检查。参数非法、必需文件缺失或关键前提不满足时说明原因，立即停止受影响流程。
- 不吞错、自动修正错误输入、降级或绕过失败，不用无算法依据的启发式后处理补救错误。
- 检查围绕当前算法、协议和实际条件，不因假想风险增加重复检查、兼容分支或阻断。

### 6. 最小充分验证与停止条件

- 仅在用户要求或修改需运行验证时做最小必要测试。先明确关键性质/完成条件，沿用标准和协议，不另建门控表、逐项审批或放行体系。
- 使用 `hif4`、合适资源与最小充分规模；可自主执行必要 GPU、真实模型和耗时验证，除用户要求或协议必需，不扩成完整训练、全量评测或无关基准。
- 覆盖可能受损的关键性质及调用链，不默认逐层、逐中间状态、穷举配置或审计底层部署；不为测试改变正式算法、添加 mock/stub 或伪造依赖。
- “能启动/没报错”不等于正确。未给容限时，按算法、精度和指标制定有依据的标准并说明；无法合理确定才询问。
- 仅因必要前提变化、相关实质异常或证据不足补验，说明其决策价值，不以“更保险”扩大检查。诊断观察不自动设门槛；仅违反算法/协议、破坏数据、无法运行或不足以有效判断的问题阻断依赖步骤，不阻断独立工作。
- 复用适用于当前代码、数据、配置的证据。关键证据充分且异常解决后立即交付，或启动已授权长实验；不降标准绕过失败、不忽略异常、不伪造结果。
- 保留必要长期回归测试，清理一次性诊断。环境/数据/资源不足时做可行检查，注明“已修改但未完成运行验证”及阻碍、范围、影响；不跳过必需验证启动正式实验。

### 7. 实验设计与后台执行

#### 设计与启动条件

- 设计/调整实验先查经验。配置搜索权衡候选数、单次预算和评测成本，提高有效比较速度，不机械沿用正式 batch、梯度累积或并行规模。
- **低成本筛选与正式条件正确性短测分开。** 已允许的近似筛选须说明并验证排序迁移风险；小 batch/短程优胜不代表大 batch/长程优胜，不授权改协议或停既有实验。
- 要求实际运行长实验时，按“完成正式代码/配置 → 充分短测 → 后台启动 → 确认推进 → 交付并停止监控”执行。只改代码/设计时不启动；关键证据缺失、验证失败或有影响运行的问题时不能“先跑再看”。
- 正式路径须有适用证据，已知长程风险须针对性验证；先完成依赖再验证和启动，不在运行中修改依赖。验证后若改动，只补验受影响路径，不沿用旧版本通过结论。

#### 正式条件下的轻量短测

- 使用正式环境、入口和真实路径，保留算法语义、必要状态、峰值资源、关键数据形状及并行条件。优先减少步数、样本数、重复次数，不靠缩模型、关键长度/批量、必要并行规模或关闭功能掩盖风险；隔离诊断不能代替正式条件验证。
- 用最少代表性数据及必要迭代覆盖实际启用流程，不只测初始化/首批，不穷举未启用功能。训练覆盖前向、反向、梯度累积与更新，推理覆盖主循环，多进程/多卡覆盖通信与同步。
- 合并检查并复用证据。中间评测后回训、保存/必要读取、换轮、阶段/调度切换、最终保存与汇总等启用的低频环节，可用少量数据、缩短触发间隔或直接进入阶段验证；不得伪造状态、另写简化实现或跳过前置条件。恢复训练只在本次包含恢复流程时验证。
- 检查必要重复迭代的内存/显存变化和已暴露的累积、未释放、阻塞问题；用相应输入/状态覆盖峰值，按实际配置与观测估算输出增长、存储需求，不做假想压力测试。
- 短测参数只压缩时间，不改变待验环节语义或关键资源条件。提前触发或执行过不等于排除风险，不足时补验缺少条件。
- 通过须以关键计算、状态变化、阶段衔接及输出正确、无相关实质异常为据；退出码或进程存活不够。短测不证明最终收敛、完整效果或外部环境永不变化，不因此无限追加检查。

#### 后台启动与资源使用

- 核对实时占用和调度要求，使用已授权资源，不默认用全部 GPU、不干扰已有任务。历史 GPU 编号/冒烟配置不自动成为后续授权或额外审批条件。
- 长任务使用现有 tmux、nohup 或等效的脱离会话方式，不为统一工具重启活动实验；后台工具不代替资源申请。短时只读/文档检查可在当前终端完成。
- 条件满足即启动已授权正式实验，不重复询问；检查正式参数，无遗留短测限制/触发频率，使用协议初始模型与状态，不沿用短测更新；短测、正式及旧结果分开存放。
- 记录任务名、实际命令/配置、PID/会话或作业 ID、日志、结果目录和退出状态；stdout/stderr 落盘、nohup 断开 stdin；不另建守护/调度/自动重试体系。
- 有限检查实际初始化、主循环计算和进度；PID/排队不等于正常，不重等已验证的低频环节。短任务查结果和退出状态；错误按修复/确认边界处理，不靠重启、降正式配置或跳过失败。后台运行或必要验证未建立时如实报告。
- 不自动占卡。用户已授权临时占卡时，只用于已安排的后续长任务间衔接；无后续任务或其暂停/取消时，按进程权限及时释放，不在最后任务结束后无条件追加占卡，不迁入其他项目的脚本名称。

#### 交付后停止监控

- **正式后台任务正常推进后立即交付并停止主动监控**，不持续轮询、不等完整实验结束、不创建监控自动化；保持任务运行并保护依赖。
- 交付验证覆盖/结果/限制、正式命令/配置、运行标识、日志和输出路径，状态写“已启动，运行中”；区分“已启动”“已完成”“结果已验证”。
- 用户再次唤起或通知完成后，先核实运行标识、日志和输出，再处理结果/失败及收尾；不默认成功或重复启动。只读查询无需再问，停止既有任务或覆盖产物仍须授权。

### 8. 工作区与实验结果

- 修改前查受影响源码、依赖和未提交差异，保留必要 diff/版本标识；不覆盖、丢弃或回滚其他任务改动。未提交不阻止工作，相关冲突或意图不明才询问。
- 不修改正式实验仍依赖的代码/配置；保护活动 progressive 的源码、结果与 checkpoint。新运行用独立输出位置，不覆盖旧结果。
- 默认保留既有结果、原始数据和模型权重。任务内可清理用完的一次性诊断与确认无用的临时缓存；删除既有产物须在本项目明确授权内。单次清理不成为持续授权，不迁入 VAELLM 的自动删除权重、淘汰分支或研究目标。
- 收尾按 **结果总结 → 经验增量检查 → 导航更新 → 授权范围内清理** 执行，覆盖完成、失败、中止或被替代任务；后台运行中只处理已结束且无引用的独立产物，不等待或监控完成。
- 清理前查内容、大小、完成/退出状态、活动进程和后续引用，不凭 tmp/test/smoke、旧日期或失败判定无用；旧目录的共享模块和第三方依赖按调用关系保护。
- 授权清理先归并结论，保留生效配置、核心日志、原始指标、复现入口和必要经验，纳入唯一版本/哈希、退出结果与对照来源。权重及恢复状态按当前导出、续训、复评用途判断，不能以完成即认定无用。
- 保护活跃/被具体后续任务使用的文件、初始模型/原始数据、指定产物、正式源码、长期测试及必要证据。依赖不明或涉及项目外共享数据先查用途，仍不明则保留说明，继续处理其他明确目标。
- 删除前记录精确路径、原因、大小，核对解析路径在授权范围内；不跟随符号链接越界、不宽泛通配或清空 home/共享/系统目录。硬链接按实际释放块统计，不累加逻辑大小。
- 删除后核对目标已移除、受保护记录与当前任务仍可用，报告内容、释放空间和保留理由；不把共享磁盘变化/他人删除量计入收益，不留无用清理脚本和重复清单，不自动暂存提交。

### 9. 代码风格与文档

- 沿用命名、类型标注和代码风格，不强制补齐无关函数；注释解释意图、难点和约束，不逐行复述或套模板。
- 新增/扩展实现保持入口薄、职责清楚，核心逻辑进合适模块；不把解析、数据、模型、训练、保存、评测全堆进一个脚本，也不借此重构无关旧代码。
- 同步修正失真的文档、示例与调用说明；复杂任务有必要才新增方案/总结，不逐步建文件。
- 新建文本用 UTF-8、执行用 shell 脚本用 LF；避免无关全文件换行变化。

### 10. 沟通与交付

- 用简单直白中文说明问题、决定与结果，解释必要术语；不用 P0/P1/P2 标签、不角色扮演、不重复背景和已解决问题。
- 目标清晰且无待确认操作时持续推进，不逐步询问是否继续；耗时工作简报关键发现、决定与真实阻碍，不播报每步操作。
- 交付相关修改、原因、实际验证与限制；修 bug 说明根因/改法，无需测试说明原因。区分事实、实测、推断，未运行不写通过、未验证不写完成；缺少源码不虚构结构/命令/依赖。
- 实验记录实际配置、种子、模型/数据版本、命令和代码版本；未提交用 diff/哈希标识，无提交不伪造 commit ID。低比特实验注明实际位宽、存储是否含 scale/zero-point/码本等，以及指标评估条件；理论压缩率、fake quant 或估算不冒充实测存储、性能或精度。

### 11. 规范维护

- 明确纠正或任务收尾时，主动合并本项目已确认的长期习惯；重复但未确认的行为只提候选建议并注明依据/条件。单次选择、临时限制、沉默及推荐项不成为永久规则或授权，跨项目推广须另行确认。
- 更新前检索并修订已有条款，冲突先核实意图；协作偏好归本文件，技术经验归经验库，具体结果归记录，不建偏好碎片/正文副本，不借规范更新扩大权限。
- 合并同义条款、删重复解释和过期例子，保留关键约束；修改后核对 UTF-8 字节数，尽量保持 15–20 KiB 并留余量，不为凑长度删关键规则或调大加载上限，不另设 AGENT.md/正文跳转文件。
- 有实质新增才在交付中一句话说明；无新增不改文档、不重复汇报，不建监控或定时总结。本条只维护本项目规范，不授权修改跨会话全局记忆。

## 二、当前项目约定

### 0. 文档与经验

- 每个任务先读 [docs/README.md](docs/README.md)，按主题读取正文，需要时追溯证据/历史；不全量加载旧计划或 `.ai-bridge/`，其交接状态不是当前实测或授权。
- 遵守 [文档管理规范](docs/MANAGEMENT.md)，指南、经验、记录、方案历史和生成报告分开；同一问题/实验优先一份正文，旧路径只导航。
- 设计/调整实验先读 [经验库](docs/experience/README.md)，按模块、损失、报错、评测目标搜 `docs/experience/lessons/`；命中后读来源与边界，简记参考经验、条件差异、规避动作、待验假设。无命中如实说明，重做失败方案须有新条件或依据。
- 经验按问题更新于 `docs/experience/lessons/`，写证据状态、原因/解释、做法、边界和来源；具体配置/指标/日志索引/结论按主题与日期放 `docs/experience/records/`，双向引用。原始指标、核心日志和生成报告留结果目录，不复制大产物。
- 收尾按第八节执行：先判断经验增量，未验证原因标假设，无新认识写“无新增经验”；不强建文件、不把冒烟通过写成方法提升。已有主题合并并注明替代关系，新主题才更新入口。
- 在 `hif4` 中用 `python tools/docs.py search '关键词'` 搜指南/经验，证据用 `--scope records`，历史用 `--scope history`；文本检索优先 `rg`，缺少时用 `grep`，不为检索装依赖。
- 新增、移动、改标题或归档后运行 `python tools/docs.py index` 和 `python tools/docs.py check`；INDEX 自动生成，不另维护手工全量目录。普通文档改动只查相关索引/链接/引用，不启动 GPU 训练或复评。
- 跨项目只迁移通用流程，保留本项目环境、模型、评测与删除授权；活动实验保持原位，历史记录不是当前状态或新的执行授权。

### 1. 运行环境

- 工作区为 `/home/shaoyuantian/program/HiF4_Sp`，直接使用当前机器的终端、文件系统和编辑器，命令遵守实际 Linux shell 语法；不要求额外建立 SSH 连接，客户端连接配置留在客户端。
- Python 命令、测试、训练、评估和 vLLM 推理统一使用 `hif4`，不得默认用 base。非交互 shell 先 `source /home/shaoyuantian/anaconda3/etc/profile.d/conda.sh`，再 `conda activate hif4`。
- 用 `which python`、`python -V` 核对解释器；已确认正确的同一 shell 可复用。环境激活由代理在调用者 shell 自主完成，不要求用户手动切换，不写进实验脚本。
- 激活失败或依赖缺失时报告阻碍，仅停止依赖它的运行并询问下一步；不擅自换环境、安装依赖或伪造存根。
- 主机/GPU/驱动/CUDA/PyTorch 信息以本项目实测为准，注明来源与日期；不复制 VAELLM 的环境版本表或将历史资源配置当作当前状态。

### 2. 下游评测

- 非 reasoning 的普通下游任务，如 ARC、MMLU，使用 `lm_eval`。
- reasoning 任务，包括 MMLU-Pro、GSM8K，使用 `lighteval`，通过仓库根目录 `main.py` 和 vLLM 运行。
- MMLU-Pro 默认 `max_samples=300`，禁止使用 `lm_eval` 运行 MMLU-Pro。

### 3. shell 脚本

- `.sh` 只设必要环境变量、明确本次 CLI 参数并调用 `python ...` 或 `python -m ...`；训练、量化、重构、评测及编排业务逻辑放 Python 模块。
- 由已激活 `hif4` 的调用者 shell 执行；脚本内不写 `conda run`（含其他选项）或 `conda activate`。此限制不妨碍代理在执行命令前激活调用者环境。
- 固定超参数直接写命令，仅路径、重复复用值或用户要求可用环境变量覆盖时使用变量，不把每个 CLI 参数做成覆盖模板。
- 互斥参数只写本次选择，替代行可注释说明；不用 shell 数组、条件分支或中间变量动态拼装，参数名以当前入口为准。
- 脚本记录一次明确配置，不建设通用模板。新增/本次确需修改的脚本按此执行，不为迁移规范改写活动实验依赖。
